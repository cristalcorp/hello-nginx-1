# hello-nginx-1
```bash
hello-nginx-1/
├── .github/
│   └── workflows/
│       └── ci.yaml              # Full CI/CD pipeline
├── index.html                   # Custom HTML
├── Dockerfile                   # nginx image with embedded index
└── README.md
```
